$(document).ready(function () {
    $('.text').textllite({
        loop: true,
        sync: true,
        in:{
            effect: "bounceln"
        },

        out:{
            effect: "bounceOut"
        }
    })
});